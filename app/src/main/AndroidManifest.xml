<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="abbos.uzeu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_engrus"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_engrus"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.second.activity.InfoActivity"></activity>
        <activity android:name=".activity.second.activity.SearchActivity" />
        <activity android:name=".activity.second.activity.FavActivity" />
        <activity android:name=".tmp.TmpActivity" />
        <activity android:name=".activity.second.activity.PhraseActivity" />
        <activity android:name=".activity.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>